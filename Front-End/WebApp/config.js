var config = {

  server: {
    port: 5000,
    domain: localhost:,
  }

  views: {
    folder: views,
    indexpage: index,
    profilePage: profile,
    registryPage: registry
  }

  static: {
    folder: public,
  }

};

module.exports = config;
